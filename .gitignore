node_modules/
.next/
dist/
out/
build/
.env
.env.local
.env*.local
*.log
.DS_Store
.turbo/
coverage/
*.tsbuildinfo
.vercel/

# Confidential source spec — kept out of the repo (marked Internal Campaign Use)
GG.pdf
GroundGame_Build_Spec.docx

# Real delegate conference data (PII) — never commit; only the derived public
# region/constituency hierarchy (apps/web/lib/hierarchy.ts) is checked in.
constituency conference/
apps/web/lib/delegates.local.json
supabase/seed_delegates.local.sql
